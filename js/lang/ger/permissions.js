export function tipText(permission) {
    return `Schreibe jetzt die Rolle für die Berechtigung ${permission}`;
}
export function wrongInput(input) {
    return `Ich konnte keine Rolle mit der ID ${input} finden.
	Bitte schreibe eine ID, oder @ die Rolle im Chat.
	
	Du kannst den Vorgang auch mit "!config cancel" abbrechen`;
}
//# sourceMappingURL=permissions.js.map